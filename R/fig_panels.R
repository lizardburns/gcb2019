# figure panel labels - grobs
a.title.grob <- textGrob(
  label = "(a)",
  x = unit(0, "lines"),
  y = unit(0, "lines"),
  hjust = 0, vjust = 0,
  gp = gpar(fontsize = 16))

b.title.grob <- textGrob(
  label = "(b)",
  x = unit(0, "lines"),
  y = unit(0, "lines"),
  hjust = 0, vjust = 0,
  gp = gpar(fontsize = 16))

c.title.grob <- textGrob(
  label = "(c)",
  x = unit(0, "lines"),
  y = unit(0, "lines"),
  hjust = 0, vjust = 0,
  gp = gpar(fontsize = 16))

d.title.grob <- textGrob(
  label = "(d)",
  x = unit(0, "lines"),
  y = unit(0, "lines"),
  hjust = 0, vjust = 0,
  gp = gpar(fontsize = 16))

e.title.grob <- textGrob(
  label = "(e)",
  x = unit(0, "lines"),
  y = unit(0, "lines"),
  hjust = 0, vjust = 0,
  gp = gpar(fontsize = 16))
