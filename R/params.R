# increment to adjust viral load zero values ahead of taking logs
increment <- 1e-4

# hex (and RGB) codes for colour-blind friendly colour palette
cTourq <- '#009292'
cViolet <- '#B66DFF'
cOrange <- '#DB6D00'
